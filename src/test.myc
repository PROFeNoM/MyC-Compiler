int main () {
    int x, y;
    x = 1;
    while (x < 5) {
        int z;
        while (y < 2) {
            y = y + 2;
            z = 2;
        }
        
        x = x + 1;
    }

    return x;
}